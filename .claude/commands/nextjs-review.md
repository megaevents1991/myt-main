Review the specified file(s) for Next.js 15 App Router best practices.

Audit each file against the always-on standard in `@.claude/rules/standards/nextjs.md`
(single source of truth). Watch the Next 15 gotchas: `await params`, GET handlers not
cached by default, ISR on order pages.

Output per file: each rule **PASS** or **FAIL [line X]** + a one-line fix.

$ARGUMENTS
