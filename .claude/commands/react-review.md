Review the specified file(s) for React 19 best practices.

Audit each file against the always-on standard in `@.claude/rules/standards/react.md`
(single source of truth). Pay extra attention to order state living in `OrderContext`
(`@.claude/rules/order-flow.md`).

Output per file: each rule **PASS** or **FAIL [line X]** + a one-line fix.

$ARGUMENTS
